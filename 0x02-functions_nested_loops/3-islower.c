#include <stdio.h>
/**
*   _islower- Entry point
* This function takes an integer parameter names c
* Return: integer (Success)
*/
int _islower(int c)
{
if (c > 96 && c < 123)
{
return (1);
}
else
{
return (0);
}
}
