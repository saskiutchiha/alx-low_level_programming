#include <stdio.h>
/**
*   _islower- Entry point
* int c is a parametre for a _islower function
* Return: integer (Success)
*/
int _islower(int c)
{
if (c > 96 && c < 123)
{
return (1);
}
else
{
return (0);
}
}
